projection implementation in class ZBP_C_GSTUOM unique;
strict ( 2 );
use draft;
define behavior for ZC_GSTUOM000 alias ZcGstuom000
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}